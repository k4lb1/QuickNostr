// Example config for QuickNostr.
// 1) Copy this file to `nostr-contact-config.js`.
// 2) Replace the values below with your own data.
// Note: `nostr-contact-config.js` is ignored by git so your personal npub
//       will not be committed to a public repository.
window.NOSTR_CONTACT_CONFIG = {
  // TODO: replace with your own npub
  NOSTR_TARGET_NPUB: "npub1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  // You can change this to any Nostr relay you prefer
  NOSTR_RELAY: "wss://nos.lol",
};

